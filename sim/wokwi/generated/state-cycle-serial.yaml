# GENERATED FILE — do not edit.
# Produced by `cargo run -p kivori-wokwi-vectors` from the real kivori-protocol
# codec; every write-serial byte array below is a genuine wire frame. CI regenerates
# this file and fails if it differs (stale vectors).
#
# External serial test: desired-state commands arrive as real SetState frames and the device
# reports back. `booting`/`offline` are unrepresentable in the payload (FR-014/FR-015). An invalid
# frame must not corrupt the current state, and the next valid frame must still be served.
# The run ends on ALL PASS state-cycle, which the firmware emits ONLY after every post-condition
# it observed genuinely held — never merely because a step executed.
name: kivori-state-cycle-serial
version: 1
author: Kivori
steps:
  - wait-serial: 'KIVORI-EXT READY iface=usb-serial-jtag'
  # hello: handshake open at the current protocol version
  - write-serial: [4, 86, 75, 1, 1, 1, 1, 1, 2, 10, 1, 2, 1, 1, 1, 10, 248, 172, 209, 145, 1, 71, 89, 198, 71, 0]
  - wait-serial: 'KIVORI-EXT RX kind=Hello'
  - wait-serial: 'KIVORI-EXT TX kind=HelloAck nonce=ok'
  # set_state_idle: desired state idle — expects StateReport(idle)
  - write-serial: [4, 86, 75, 1, 1, 1, 2, 2, 2, 3, 2, 4, 1, 5, 53, 161, 82, 241, 0]
  - wait-serial: 'KIVORI-EXT RX kind=SetState'
  - wait-serial: 'KIVORI-EXT TX kind=StateReport reported=idle'
  - wait-serial: 'KIVORI-EXT STATE current=idle'
  # set_state_happy: desired state happy — expects StateReport(happy)
  - write-serial: [4, 86, 75, 1, 1, 1, 2, 3, 2, 3, 3, 4, 1, 5, 192, 155, 62, 78, 0]
  - wait-serial: 'KIVORI-EXT TX kind=StateReport reported=happy'
  - wait-serial: 'KIVORI-EXT STATE current=happy'
  # malformed_cobs: invalid COBS run length — must be dropped without panic (SC-008)
  - write-serial: [5, 255, 1, 0]
  - wait-serial: 'KIVORI-EXT DROP reason=decode'
  - wait-serial: 'KIVORI-EXT STATE current=happy'
  # ping: heartbeat — expects a Pong echoing the timestamp
  - write-serial: [4, 86, 75, 1, 1, 1, 2, 1, 2, 3, 4, 6, 146, 33, 4, 151, 32, 204, 0]
  - wait-serial: 'KIVORI-EXT TX kind=Pong'
  - wait-serial: 'KIVORI-EXT ALL PASS state-cycle'
