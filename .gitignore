# Rust
/target
**/target
*.rs.bk
Cargo.lock.bak

# Node / frontend
node_modules
dist
.vite
*.tsbuildinfo

# Generated / compiled assets (source SVGs are tracked; compiled runtime blobs are not)
/assets/compiled

# Tauri-generated capability schemas + codegen
**/src-tauri/gen/

# Env / secrets / OS / editors
.env
.env.*
.DS_Store
Thumbs.db
.idea
.vscode

# Captured application stdout from a validation attempt is a runtime artifact, not evidence.
docs/evidence/*-app-stdout.log

# Superpowers SDD scratch workspace (ledger, briefs, review packages)
.superpowers/
